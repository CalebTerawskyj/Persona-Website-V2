---
const blogPosts = [
{ title: "My Homelab", slug: "my-homelab", date: "2025-12-15", tags: ["homelab", "networking"], readTime: "8 min" },
];
---

<section id="blog" class="py-24 px-6">
<div class="max-w-6xl mx-auto">

    <div class="flex items-end justify-between mb-12">
    <div>
        <span class="text-xs tracking-widest uppercase mb-3 block
                    text-cyan-600/70 dark:text-cyan-400/70"
            style="font-family: 'JetBrains Mono', monospace;">Writing</span>
        <h2 class="text-3xl md:text-4xl font-bold
                text-slate-900 dark:text-slate-100"
            style="font-family: 'JetBrains Mono', monospace;">Latest Posts</h2>
    </div>
    <a href="/blog" class="hidden md:block text-sm transition-colors
                            text-slate-400 dark:text-slate-500
                            hover:text-cyan-600 dark:hover:text-cyan-400"
        style="font-family: 'DM Sans', sans-serif;">
        View all →
    </a>
    </div>

<!--
    <div class="space-y-1">
    {blogPosts.map((post) => (
        <a href={`/blog/${post.slug}`}
        class="group flex flex-col sm:flex-row sm:items-center justify-between 
                py-5 px-4 -mx-4 rounded-lg transition-all duration-300 
                border-b last:border-0
                hover:bg-slate-50 dark:hover:bg-slate-900/50
                border-slate-100 dark:border-slate-800/50">
        <div class="flex-1 min-w-0 mb-2 sm:mb-0">
            <h3 class="text-base font-medium transition-colors duration-200 mb-1 truncate
                    text-slate-700 dark:text-slate-300
                    group-hover:text-cyan-600 dark:group-hover:text-cyan-400"
                style="font-family: 'JetBrains Mono', monospace;">
            {post.title}
            </h3>
            <div class="flex items-center gap-3">
            <span class="text-xs text-slate-400 dark:text-slate-600"
                    style="font-family: 'JetBrains Mono', monospace;">{post.date}</span>
            <span class="text-slate-300 dark:text-slate-800">·</span>
            <span class="text-xs text-slate-400 dark:text-slate-600"
                    style="font-family: 'DM Sans', sans-serif;">{post.readTime} read</span>
            </div>
        </div>
        <div class="flex gap-2 shrink-0">
            {post.tags.map((tag) => (
            <span class="text-xs px-2 py-0.5 rounded-full
                        bg-slate-100 dark:bg-slate-800
                        text-slate-500 dark:text-slate-500"
                    style="font-family: 'JetBrains Mono', monospace;">{tag}</span>
            ))}
        </div>
        </a>
    ))}
    </div>
    -->
</div>
</section>
